
struct Point {
    h: uint64;
    x: uint32;
    y: uint32;
}

table PointTable {
    h: uint64;
    x: uint32;
    y: uint32;
}

table Feature {
    points: [Point];
}

// Just adds functions that verify a given table is the root
// root_type PointTable;
